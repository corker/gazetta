template: code-block
content:
  language: bash
  code: |-
    npx gazetta init my-site
    cd my-site
    npm install
    npx gazetta dev
    # → http://localhost:3000 (site)
    # → http://localhost:3000/admin (CMS editor)
