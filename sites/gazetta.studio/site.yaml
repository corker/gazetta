name: "Gazetta Studio"
targets:
  production:
    type: s3
    endpoint: "http://localhost:9000"
    bucket: "gazetta-studio"
    accessKeyId: "minioadmin"
    secretAccessKey: "minioadmin"
    region: "us-east-1"
