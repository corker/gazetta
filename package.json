{
  "name": "@gazetta/root",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*",
    "sites/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/gazetta",
    "dev": "npm run build && npm run dev -w examples/starter",
    "dev:admin": "npm run build && npm run dev:api -w @gazetta/admin-ui & npm run dev:ui -w @gazetta/admin-ui",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
