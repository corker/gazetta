# @gazetta/renderer

Hono-based rendering engine for Gazetta. Walks the component tree, executes templates, composes pages with CSS scoping.

Includes: site loader, resolver, renderer, template loader, filesystem and Azure Blob storage providers.

See [gazetta-studio](https://github.com/gazetta-studio/gazetta-studio) for the full project.
